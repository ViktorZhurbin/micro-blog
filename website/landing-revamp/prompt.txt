You're rewriting the landing page for Castro, a static site generator (Preact + Bun) with Soviet-satire branding. I'm giving you two source documents. Read both fully before writing anything.

DOCUMENT 1 — "landing-history-context.md": describes the landing page's copy across its past eras (satirical → serious → educational → minimalist), with recovered material — old headlines, directive slogans, island-example concepts, hero taglines — that could be reused or adapted. This is reference material, not an argument for a particular direction. Where it lists open questions or unranked options, those are genuinely open — form your own view.

DOCUMENT 2 — "voice.md": the rules governing Castro's satirical voice in its actual error messages and CLI output. The landing page should sound like it was written by the same voice as this file — not a separate, louder marketing register.

THE DIRECTION (this is not fully reflected in Document 1 — don't default to what it emphasizes just because it's thoroughly documented):

Register: go back to the commitment level of the original landing page quoted in Document 1 — fully committed, no hedging, joke lives in proper nouns and headers, technical prose underneath stays plain.

Scope: this is no longer "learn island architecture" — that's a five-minute concept, too narrow to be the whole pitch. Reframe around static site generation / the framework as a whole, with islands as Castro's specific mechanism within that, not the entire subject of the page.

Tagline: use "Your Five-Year Plan to Understand Your Framework" as the hero tagline (or a close variant of it — small wording changes are fine, the structure and payload are not up for reinvention).

Critical nuance on that tagline — read Document 1's "Whose framework" section for the full reasoning, but the short version: "your" is ambiguous between "the framework you, the visitor, will use" and "the framework I, the author, built myself." The second reading is the one that carries the joke — the self-irony that anyone can build one of these now, with an LLM's help, and the person who made this one knows it. The tagline alone won't force that reading. You need to land it with something nearby — a kicker line, a subline, the sentence right under the hero — that makes clear the "you" is the author, not a generic second-person visitor. If you don't do this, the tagline just reads as ordinary SaaS second-person address wearing a communist costume, which is the exact flat outcome this rework is trying to avoid.

Self-irony, generally: working software, explicitly not serious software. Own the fact that this is a personal framework built by one person, in an era where that's trivially easy to do — don't undercut that with false modesty, but don't oversell the tool either.

WHAT'S STILL OPEN — make actual choices, don't hedge or list alternatives:

- Directive naming: Document 1 shows both an original irreverent naming (`no:pasaran`, `lenin:awake`, etc.) and a later "sober" naming. Pick one direction and commit.
- Whether `no:pasaran` (or an equivalent "this isn't really a directive, it's just not using an island" joke) returns.
- Hero layout: how kicker / tagline / quote line combine.

ON ISLAND EXAMPLES — genuine creative latitude here: Document 1 describes three past attempts (PropagandaRadio, FiveYearPlan, Redactor). None of them are working well on the actual page today. Use them only as reference for tone and mechanics (what makes a satirical interactive toy land vs. feel like a gimmick) — you are not restricted to picking one. If you have a better concept for a live interactive demo that fits the page's voice, propose that instead, and note in one line why it's a better fit than the three precedents.

WHAT I WANT FROM YOU:

A skeleton for the new landing page in plain markdown — no HTML, no design commentary, no explanation of your choices. Use the shape/format as a starting point (its content is exactly the island-only framing this brief is moving away from, don't reuse it):

> **[star emblem]**
>
> # Castro
>
> ### The People's Framework
>
> **The Educational Island Architecture Framework**
> **(That Happens to Be Communist)**
>
> _"The satire is optional. The knowledge is real."_
>
> **[Read the Manifesto]** **[View Source Code]** **[Start Tutorial]**
>
> ---
>
> ## The Revolutionary Directives
>
> Learn how modern SSGs work by reading ~1500 lines of well-commented code.
> Three hydration strategies. Zero configuration.
>
> **no:pasaran** — _"They shall not pass (to the client)"_
> Component renders at build time. No JavaScript shipped to client. Pure
> static HTML for maximum performance.
> _[live counter demo]_
> ↑ Try clicking. Nothing happens. Zero JS was sent to your browser.
>
> **lenin:awake** — _"The leader is always ready"_
> Component becomes interactive immediately on page load. Full
> interactivity from the start.
> _[live counter demo]_
> ↑ This counter is interactive immediately. JS loaded on page load.
>
> **comrade:visible** — _"Only work when the people are watching"_
> Component hydrates when scrolled into viewport. Lazy loading with
> IntersectionObserver. Default behavior.
> _[live counter demo]_
> ↑ JS loads when scrolled into view. Open DevTools Network tab to verify.
>
> ---
>
> ## How The Revolution Works
>
> Island architecture explained. No magic, just smart progressive
> enhancement.
>
> **1 — Build Time**
> Castro compiles your pages and renders all islands to static HTML. Every
> component gets server-side rendered, creating instant visual content.
>
> **2 — Browser Receives HTML**
> Pure HTML arrives first. Your page is visible immediately. No waiting for
> JavaScript bundles. Islands are wrapped in `<castro-island>` custom
> elements.
>
> **3 — Selective Hydration**
> JavaScript loads based on your directive. `no:pasaran` stays static.
> `lenin:awake` hydrates immediately. `comrade:visible` waits for viewport
> intersection.
>
> **4 — Interactive Islands**
> Components become interactive exactly when needed. Fast initial load,
> progressive enhancement, minimal JavaScript. This is island architecture.
>
> ---
>
> **Workers of the Web, Unite!**
> **Seize the Means of Rendering.**
>
> [About] [GitHub]
>
> _Built with Castro | The People's Framework_

Requirements for the copy itself:

- Apply the grandiosity-gap principle: grand ideological language paired with a small technical event, never grand-on-grand or flat-on-flat.
- Keep prose sections scannable — satire at the edges (headers, kickers, punchlines), plain and accurate in the middle (technical explanations of how it works).
- New words throughout — don't reuse the original page's phrasing verbatim — but the same discipline behind it: one joke per beat, cut it if it doesn't land.

Output: just the markdown skeleton, plus (if applicable) a short note on a replacement island example. No other preamble, no rationale, no alternate versions.
